# Application Environment
NODE_ENV=development

# Database Configuration
# For development: :memory: (in-memory database)
# For production: /data/restaurant.db (persistent file)
DB_PATH=:memory:

# Server Configuration
PORT=3000

# Log Level (optional)
# LOG_LEVEL=debug
